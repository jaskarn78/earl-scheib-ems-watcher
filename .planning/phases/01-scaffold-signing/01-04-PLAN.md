---
phase: 01-scaffold-signing
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - .github/workflows/build.yml
  - Makefile
  - docs/cert-procurement.md
autonomous: false
requirements:
  - SCAF-03
  - SCAF-06

user_setup:
  - service: code-signing-ca
    why: "OV code-signing certificate required for Authenticode signing (SCAF-03, SCAF-06)"
    env_vars:
      - name: SIGNING_CERT_B64
        source: "CA portal (DigiCert/Sectigo/SSL.com) → export PFX → base64-encode: `base64 -w0 signing.pfx`"
      - name: SIGNING_CERT_PASS
        source: "Password you set when exporting the PFX from the CA portal"
      - name: GSD_HMAC_SECRET
        source: "The HMAC secret key shared with the webhook server (already in use by Python watcher)"
    dashboard_config:
      - task: "Purchase OV code-signing certificate"
        location: "DigiCert (https://www.digicert.com/signing/code-signing-certificates) or Sectigo via reseller (~$200-225/yr)"
      - task: "Complete OV identity validation"
        location: "CA will email validation steps after purchase; allow 2-10 business days"
      - task: "Export PFX from CA portal or cloud HSM"
        location: "DigiCert Trust Manager or SSL.com eSigner dashboard → export certificate with private key as .pfx"
      - task: "Add three secrets to GitHub repository"
        location: "GitHub repository → Settings → Secrets and variables → Actions → New repository secret"

must_haves:
  truths:
    - "When `SIGNING_CERT_B64` secret is present in CI, the workflow signs the exe with `osslsigncode` before uploading the artifact"
    - "When `SIGNING_CERT_B64` secret is absent (dev builds, forks), the workflow skips signing and uploads the unsigned exe — CI does not fail"
    - "The signing step uses a RFC 3161 timestamp server so the signature remains valid after certificate expiry"
    - "A `make dev-sign` target creates a self-signed cert and signs locally for end-to-end pipeline testing without a real OV cert"
    - "A cert procurement document exists tracking the steps required to obtain and provision the OV cert before Phase 4"
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "CI workflow with conditional osslsigncode signing step"
      contains: "osslsigncode sign"
    - path: "Makefile"
      provides: "dev-sign target for local self-signed signing tests"
      contains: "dev-sign"
    - path: "docs/cert-procurement.md"
      provides: "Step-by-step OV cert procurement and HSM provisioning checklist"
      contains: "DigiCert"
  key_links:
    - from: ".github/workflows/build.yml sign step"
      to: "dist/earlscheib.exe (unsigned)"
      via: "osslsigncode sign -pkcs12 signing.pfx"
      pattern: "osslsigncode sign"
    - from: ".github/workflows/build.yml sign step"
      to: "dist/earlscheib-signed.exe (signed artifact)"
      via: "osslsigncode -out dist/earlscheib-signed.exe"
      pattern: "earlscheib-signed.exe"
    - from: "SIGNING_CERT_B64 secret"
      to: "signing.pfx (ephemeral CI file)"
      via: "echo $SIGNING_CERT_B64 | base64 -d > signing.pfx"
      pattern: "base64 -d.*signing.pfx"
---

<objective>
Add Authenticode signing to the CI pipeline using osslsigncode with a conditional dev-sign fallback. When the `SIGNING_CERT_B64` secret is absent, the build still completes (unsigned exe). When present, the exe is signed before upload. Adds a `make dev-sign` target using a self-signed cert so the full pipeline can be tested end-to-end locally and in CI without a real OV cert.

Creates `docs/cert-procurement.md` — a checklist tracking the OV cert procurement steps that MUST complete before Phase 4 ships (SCAF-06).

Purpose: Signs every CI build from day one, progressively building SmartScreen reputation. The dev-sign fallback means this pipeline is testable immediately; the real signing activates the moment the OV cert secrets are added to the repository.

Output: Extended `.github/workflows/build.yml`, extended `Makefile`, `docs/cert-procurement.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-signing/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-scaffold-signing/01-02-SUMMARY.md
@.planning/phases/01-scaffold-signing/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 output (01-02-SUMMARY.md): -->
<!--   .github/workflows/build.yml already has: checkout, setup-go, vet, make build-windows, upload-artifact -->
<!--   Artifact name: earlscheib-windows-amd64 -->
<!--   Secret name for HMAC: GSD_HMAC_SECRET -->

<!-- From Plan 03 output (01-03-SUMMARY.md): -->
<!--   make build-windows now calls generate-resources first -->
<!--   dist/earlscheib.exe is the unsigned output -->

<!-- osslsigncode signing pattern (from STACK.md lines 100-104): -->
<!--   echo "$PFX_B64" | base64 -d > signing.pfx                                               -->
<!--   osslsigncode sign -pkcs12 signing.pfx -pass "$PFX_PASS"                                 -->
<!--     -n "Earl Scheib EMS Watcher"                                                           -->
<!--     -i "https://support.jjagpal.me"                                                        -->
<!--     -t http://timestamp.digicert.com                                                       -->
<!--     -in dist/earlscheib.exe -out dist/earlscheib-signed.exe                               -->
<!--   osslsigncode verify -in dist/earlscheib-signed.exe  (check: "Signature verification: ok") -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add conditional osslsigncode signing step to CI workflow</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .github/workflows/build.yml (current state from Plan 02 — must preserve all existing steps, only add signing)
    - .planning/research/STACK.md (lines 100–104 — exact osslsigncode command pattern)
    - .planning/research/PITFALLS.md (lines 323–343 — Pitfall 14: cert procurement, HSM requirement, signing workflow)
  </read_first>
  <action>
Read `.github/workflows/build.yml` in full, then add these steps AFTER the existing `Build windows/amd64` step and BEFORE the `Upload artifact` step.

The signing block uses an `if: env.SIGNING_CERT_B64 != ''` condition so it is skipped when the secret is absent (unsigned builds still work — no CI failure).

The upload step must be updated to upload `dist/earlscheib-signed.exe` when it exists, otherwise fall back to `dist/earlscheib.exe`.

Insert these steps (preserving all existing steps verbatim):

```yaml
      - name: Install osslsigncode
        run: |
          sudo apt-get install -y osslsigncode

      - name: Sign binary (skipped if cert secret absent)
        if: env.SIGNING_CERT_B64 != ''
        env:
          SIGNING_CERT_B64: ${{ secrets.SIGNING_CERT_B64 }}
          SIGNING_CERT_PASS: ${{ secrets.SIGNING_CERT_PASS }}
        run: |
          echo "$SIGNING_CERT_B64" | base64 -d > signing.pfx
          osslsigncode sign \
            -pkcs12 signing.pfx \
            -pass "$SIGNING_CERT_PASS" \
            -n "Earl Scheib EMS Watcher" \
            -i "https://support.jjagpal.me" \
            -t http://timestamp.digicert.com \
            -in dist/earlscheib.exe \
            -out dist/earlscheib-signed.exe
          osslsigncode verify -in dist/earlscheib-signed.exe
          rm -f signing.pfx
        env:
          SIGNING_CERT_B64: ${{ secrets.SIGNING_CERT_B64 }}
          SIGNING_CERT_PASS: ${{ secrets.SIGNING_CERT_PASS }}

      - name: Prepare artifact (signed if available, unsigned otherwise)
        run: |
          if [ -f dist/earlscheib-signed.exe ]; then
            cp dist/earlscheib-signed.exe dist/earlscheib-artifact.exe
            echo "Using signed artifact"
          else
            cp dist/earlscheib.exe dist/earlscheib-artifact.exe
            echo "Using unsigned artifact (no signing cert configured)"
          fi
```

Update the existing `Upload artifact` step's `path:` from `dist/earlscheib.exe` to `dist/earlscheib-artifact.exe`.

CRITICAL: The `Sign binary` step has `env:` declared twice (once at step level for the `if:` evaluation, once inside the `run:` block). GitHub Actions evaluates `env:` at step level for the `if:` condition — keep the step-level `env:` block. Remove the duplicate inner env block from the `run:` block (environment variables at step level are automatically available in `run:`).

Correct final shape of the sign step:
```yaml
      - name: Sign binary (skipped if cert secret absent)
        if: env.SIGNING_CERT_B64 != ''
        env:
          SIGNING_CERT_B64: ${{ secrets.SIGNING_CERT_B64 }}
          SIGNING_CERT_PASS: ${{ secrets.SIGNING_CERT_PASS }}
        run: |
          echo "$SIGNING_CERT_B64" | base64 -d > signing.pfx
          osslsigncode sign \
            -pkcs12 signing.pfx \
            -pass "$SIGNING_CERT_PASS" \
            -n "Earl Scheib EMS Watcher" \
            -i "https://support.jjagpal.me" \
            -t http://timestamp.digicert.com \
            -in dist/earlscheib.exe \
            -out dist/earlscheib-signed.exe
          osslsigncode verify -in dist/earlscheib-signed.exe
          rm -f signing.pfx
```
  </action>
  <verify>
    <automated>cd /home/jjagpal/projects/earl-scheib-followup && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build.yml')); steps=[s['name'] for s in d['jobs']['build-windows']['steps']]; print(steps); assert any('osslsigncode' in s for s in steps), 'missing osslsigncode step'; assert any('artifact' in s.lower() for s in steps), 'missing artifact step'" && echo "workflow valid"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/build.yml` still parses as valid YAML
    - File contains `osslsigncode sign`
    - File contains `osslsigncode verify`
    - File contains `secrets.SIGNING_CERT_B64`
    - File contains `secrets.SIGNING_CERT_PASS`
    - File contains `if: env.SIGNING_CERT_B64 != ''` (conditional signing)
    - File contains `timestamp.digicert.com` (RFC 3161 timestamp server)
    - File contains `rm -f signing.pfx` (ephemeral PFX cleanup)
    - File contains `earlscheib-artifact.exe` (the artifact path used for upload)
    - The upload step's `path:` is `dist/earlscheib-artifact.exe`
    - File does NOT contain `signing.pfx` as an artifact upload path
  </acceptance_criteria>
  <done>CI workflow signs the exe when `SIGNING_CERT_B64` secret is present; skips signing when absent; verifies signature before upload; cleans up ephemeral PFX</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add dev-sign Makefile target and cert procurement doc</name>
  <files>Makefile, docs/cert-procurement.md</files>
  <read_first>
    - Makefile (current state from Plans 01 + 03 — must preserve all existing targets)
    - .planning/research/PITFALLS.md (lines 323–343 — Pitfall 14: CA procurement steps, 2-10 day lead time, HSM requirement)
    - .planning/research/STACK.md (lines 178–188 — Code Signing Decision table: OV recommendation, cost, Linux CI compatibility)
    - .planning/STATE.md (decisions section: "OV cert procurement must begin at kickoff — 2–10 business day lead time; use cloud HSM")
  </read_first>
  <action>
**Part A — Makefile `dev-sign` target**

Read the Makefile, then append this target. It creates a self-signed cert using `openssl` and signs with `osslsigncode` so the full signing pipeline can be tested without a real OV cert:

```makefile
## dev-sign: sign dist/earlscheib.exe with a self-signed cert (local testing only)
## Requires: openssl, osslsigncode  (sudo apt-get install openssl osslsigncode)
## Output: dist/earlscheib-signed.exe
dev-sign: build-windows
	@echo "Generating self-signed dev certificate..."
	openssl req -new -x509 -newkey rsa:2048 -keyout /tmp/dev-signing.key \
	  -out /tmp/dev-signing.crt -days 1 -nodes \
	  -subj "/CN=EarlScheibDevSign/O=DevOnly/C=US" 2>/dev/null
	openssl pkcs12 -export -out /tmp/dev-signing.pfx \
	  -inkey /tmp/dev-signing.key -in /tmp/dev-signing.crt \
	  -passout pass:devpass 2>/dev/null
	osslsigncode sign \
	  -pkcs12 /tmp/dev-signing.pfx \
	  -pass devpass \
	  -n "Earl Scheib EMS Watcher (DEV)" \
	  -i "https://support.jjagpal.me" \
	  -in dist/earlscheib.exe \
	  -out dist/earlscheib-signed.exe
	osslsigncode verify -in dist/earlscheib-signed.exe
	rm -f /tmp/dev-signing.key /tmp/dev-signing.crt /tmp/dev-signing.pfx
	@echo "Dev-signed artifact: dist/earlscheib-signed.exe"
	@echo "NOTE: self-signed cert — SmartScreen will block this on Windows. For production use real OV cert."
```

**Part B — `docs/cert-procurement.md`**

Create `docs/` directory and write this file:

```markdown
# OV Code-Signing Certificate — Procurement Checklist

**Must complete before Phase 4 ships.**
Lead time: 2–10 business days (CA identity validation).

## Why This Is Required

Every CI build is Authenticode-signed (SCAF-03). Without a real OV cert:
- Windows SmartScreen shows a red block dialog for all installs.
- Marco (non-technical end user) will be unable to run the installer.
- Phase 4 is blocked until this is done.

## Step-by-Step Checklist

### Step 1 — Purchase Certificate

- [ ] Buy an OV Code Signing Certificate from Sectigo via a reseller (~$200–225/yr) or DigiCert directly (~$370/yr)
  - Recommended resellers: Certera (https://certera.com), SSLDragon, NameCheap
  - Search for "Sectigo OV Code Signing Certificate" — avoid EV (same SmartScreen post-2024, higher cost)
- [ ] Note: as of February 2026, maximum certificate lifespan is 1 year (CA/B Forum rule)
- [ ] Set a calendar reminder 60 days before the expiry date

### Step 2 — Complete OV Identity Validation

- [ ] CA will email validation steps within 24 hours of purchase
- [ ] OV requires: business name verification, address, phone number
  - Documents typically needed: government-issued ID, business registration
- [ ] Allow 2–10 business days for validation
- [ ] Track validation status in CA portal

### Step 3 — Provision into Cloud HSM (for CI non-interactive signing)

**Preferred: SSL.com eSigner or DigiCert KeyLocker (cloud HSM — no USB token required)**

- [ ] After cert issuance, provision into cloud HSM via CA portal
- [ ] Export the certificate + key as a password-protected PFX:
  ```
  base64 -w0 signing.pfx > signing.pfx.b64
  ```
- [ ] Store the base64 value as GitHub Actions repository secret:
  - Secret name: `SIGNING_CERT_B64`
  - Location: GitHub repo → Settings → Secrets and variables → Actions
- [ ] Store the PFX password as:
  - Secret name: `SIGNING_CERT_PASS`
- [ ] Store the HMAC webhook secret as (if not already done):
  - Secret name: `GSD_HMAC_SECRET`

### Step 4 — Verify in CI

- [ ] Push a commit to trigger the CI workflow
- [ ] Confirm the `Sign binary` step runs (check Actions log)
- [ ] Confirm `osslsigncode verify` outputs `Signature verification: ok`
- [ ] Download the artifact and test on a Windows 10 VM:
  - Right-click `earlscheib.exe` → Properties → Digital Signatures tab → verify publisher name shows "Earl Scheib Auto Body Concord"
  - Run `signtool verify /pa /v earlscheib.exe` → confirm valid Authenticode signature

### Step 5 — Submit for SmartScreen Reputation (after first real signed release)

- [ ] Submit binary to Microsoft Security Intelligence: https://www.microsoft.com/en-us/wdsi/filesubmission
- [ ] Mark as "Legitimate software that I believe has been incorrectly detected"
- [ ] Submit to VirusTotal (https://www.virustotal.com/gui/home/upload) to propagate to downstream AV vendors

## Status

| Step | Status | Notes |
|------|--------|-------|
| Certificate purchased | [ ] Pending | Start immediately — 2–10 day lead time |
| OV validation complete | [ ] Pending | |
| PFX exported and base64-encoded | [ ] Pending | |
| `SIGNING_CERT_B64` secret added to GitHub | [ ] Pending | |
| `SIGNING_CERT_PASS` secret added to GitHub | [ ] Pending | |
| CI signing step verified in Actions log | [ ] Pending | |
| Signature verified on Windows VM | [ ] Pending | Required before Phase 4 ships |
| SmartScreen submission sent | [ ] Pending | After first release |

## Notes

- The self-signed dev cert (`make dev-sign`) proves the pipeline works end-to-end but produces a SmartScreen-blocked binary.
- Real OV signing activates automatically once `SIGNING_CERT_B64` and `SIGNING_CERT_PASS` secrets are added to the repository.
- Phase 4 **MUST NOT** ship until "Signature verified on Windows VM" is checked above (per SCAF-06).
- **Never commit the .pfx file to source control** — store it only as a GitHub secret.
```
  </action>
  <verify>
    <automated>cd /home/jjagpal/projects/earl-scheib-followup && grep -q 'dev-sign' Makefile && echo "dev-sign target present" && test -f docs/cert-procurement.md && grep -q 'SIGNING_CERT_B64' docs/cert-procurement.md && echo "cert doc present"</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains `dev-sign` target
    - `Makefile` `dev-sign` target contains `osslsigncode sign`
    - `Makefile` `dev-sign` target contains `osslsigncode verify`
    - `Makefile` `dev-sign` target contains `openssl req` (self-signed cert generation)
    - `Makefile` `dev-sign` target cleans up temp files (`rm -f /tmp/dev-signing.*`)
    - `docs/cert-procurement.md` exists
    - `docs/cert-procurement.md` contains `SIGNING_CERT_B64`
    - `docs/cert-procurement.md` contains `SIGNING_CERT_PASS`
    - `docs/cert-procurement.md` contains `Phase 4 MUST NOT ship` (gate condition)
    - `docs/cert-procurement.md` contains `signtool verify /pa /v`
    - `docs/cert-procurement.md` contains a status table with checkboxes
  </acceptance_criteria>
  <done>`make dev-sign` generates a self-signed cert and produces a verifiable `dist/earlscheib-signed.exe`; `docs/cert-procurement.md` tracks all steps needed to provision a real OV cert before Phase 4</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 1 scaffold:
    - Plan 01: Go module + dispatcher (all 6 subcommand stubs, HMAC ldflags)
    - Plan 02: GitHub Actions cross-compile workflow (ubuntu-latest, uploads artifact)
    - Plan 03: go-winres resources (ProductName, manifest, placeholder icon)
    - Plan 04: Conditional osslsigncode signing in CI + dev-sign Makefile target + cert procurement doc
  </what-built>
  <how-to-verify>
    1. Run `make build-windows` locally — confirm `dist/earlscheib.exe` is produced (size > 1 MB)
    2. Run `make dev-sign` — confirm `dist/earlscheib-signed.exe` is produced, `osslsigncode verify` shows `Signature verification: ok`
    3. Run `file dist/earlscheib.exe` — confirm output includes `PE32+ executable` and `x86-64`
    4. Run `strings dist/earlscheib.exe | grep -i "earl scheib"` — confirm at least one match (version resource embedded)
    5. Run `./dist/earlscheib --scan` (Linux build) or inspect the Windows exe on a VM — confirm exit 0 with "not yet implemented" message
    6. Run `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build.yml'))"` — confirm no YAML parse error
    7. Review `docs/cert-procurement.md` — confirm it captures all steps needed to provision a real OV cert
    8. Confirm `.gitignore` excludes `dist/*.exe`, `rsrc_windows_amd64.syso`, and `*.pfx`
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1 and proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:

1. `go build ./cmd/earlscheib` passes on Linux
2. `make build-windows` produces `dist/earlscheib.exe` (PE32+ windows/amd64)
3. `make dev-sign` produces `dist/earlscheib-signed.exe` with `Signature verification: ok`
4. `.github/workflows/build.yml` is valid YAML with conditional signing step
5. `winres/winres.json` has ProductName "Earl Scheib EMS Watcher"
6. `docs/cert-procurement.md` has procurement checklist with Phase 4 gate
7. All 6 subcommands print "not yet implemented" and exit 0
8. `GSD_HMAC_SECRET` env var is wired via ldflags in Makefile and CI workflow
</verification>

<success_criteria>
- CI workflow signs the exe when `SIGNING_CERT_B64` is present, skips gracefully when absent
- `make dev-sign` provides a self-signed test path (no OV cert needed to verify the pipeline)
- Cert procurement checklist exists and references the Phase 4 gate (SCAF-06)
- No PFX or private key appears in git-tracked files
- osslsigncode verify confirms signature validity in both CI and local dev-sign paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-signing/01-04-SUMMARY.md` with:
- CI signing step added (conditional on SIGNING_CERT_B64 secret)
- Secret names required: SIGNING_CERT_B64, SIGNING_CERT_PASS, GSD_HMAC_SECRET
- dev-sign target usage
- Cert procurement doc path
- Phase 4 gate: cert must be provisioned before Phase 4 ships (SCAF-06)
- Any deviations from this plan
</output>
