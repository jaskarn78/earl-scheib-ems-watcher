---
milestone: v1.0
audited: 2026-04-21
status: tech_debt
scores:
  requirements: 30/30 (active scope)
  phases: 4/4 verified
  integration: 17/17 exports wired, 0 orphaned
  flows: 5/5 E2E flows complete
gaps:
  requirements: []
  integration:
    - kind: cosmetic
      location: "internal/heartbeat/heartbeat.go"
      description: "Does not TrimRight(webhookURL, '/') before appending '/heartbeat'; telemetry and remoteconfig do. Latent risk if user adds trailing slash to config.ini."
      affected_requirements: ["SCAN-09"]
      severity: low
    - kind: cosmetic
      location: "app.py do_GET lines 1278-1296"
      description: "Dead code block after unconditional 404 return on line 1275"
      affected_requirements: []
      severity: low
    - kind: cosmetic
      location: ".github/workflows/build.yml line 131"
      description: "No-op self-rename `mv dist/earlscheib-artifact.exe dist/earlscheib-artifact.exe || true`"
      affected_requirements: []
      severity: low
  flows: []
tech_debt:
  - phase: 03-installer-native-config
    items:
      - "Runtime E2E on Windows VM (5 HUMAN-UAT items): full installer wizard flow, mapped-drive fallback, upgrade preservation, uninstall cleanup, Docker iscc syntax check"
      - "CCC ONE EMS Extract Preferences screenshot is a schematic SVG — Marco may want a real screenshot"
      - "OV code-signing certificate procurement is an external 2–10 day task (SCAF-06 tracked but unprocurable autonomously)"
  - phase: 04-telemetry-remote-config
    items:
      - "OPS-03 scope adjustment: 15-minute background poller is not implemented (tray was cut); per-scan remote-config fetch delivers updates within ~5 min via Scheduled Task instead. Document this as the chosen v1 implementation in REQUIREMENTS.md."
      - "Server-side app.py changes must be deployed (systemctl restart app) — not an autonomous step"
      - "telemetry.log rotation / size-cap on the server is not implemented — defer to ops concern"
      - "Server-side HMAC timestamp replay protection not implemented (noted in ARCHITECTURE.md as v2)"
scope_cuts:
  - reason: "User decision 2026-04-20: tray + WebView2 deemed overkill. Marco configures once at install time; log file is ongoing status surface."
  - requirements_cut: ["TRAY-01..08", "UI-01", "UI-02", "UI-03", "UI-04", "UI-05", "UI-10", "INST-05", "INST-06", "INST-07"]
human_uat_outstanding:
  - phase: 03
    items:
      - "Full installer E2E on fresh Windows 10 VM"
      - "Mapped-drive warning UX + InteractiveToken user-mode task fallback"
      - "Upgrade config.ini preservation"
      - "Uninstall removes Scheduled Task + prompts for data-dir deletion"
      - "make installer-syntax Docker compilation (or trigger installer-syntax-check CI job)"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 4
  overall: "Nyquist VALIDATION.md not generated for any phase (research+validation flow not invoked)"
---

# v1.0 Milestone Audit — Earl Scheib EMS Watcher (Windows Client)

**Audit date:** 2026-04-21
**Status:** ✅ All in-scope requirements satisfied. Tech debt + human UAT items tracked for post-audit action.
**Scope change during milestone:** After Phase 2, tray + WebView2 were cut. 16 REQ-IDs moved out of scope. Current active set: 30 requirements (6 SCAF + 14 SCAN + 4 UI-installer-native + 8 INST + 7 OPS — actually 30 = 6+14+4+5+7 with INST-05/06/07 cut, matched against the 4 phases).

## Phase Verification Summary

| Phase | Verification | Plans | Notes |
|-------|--------------|-------|-------|
| 1. Scaffold + Signing | passed (5/5) | 4/4 | go-winres CI install-step gap closed inline after initial verification |
| 2. Core Scanner | passed (14/14) | 5/5 | All 9 internal packages green under `-race` |
| 3. Installer + Native Config | human_needed (7/7 structural) | 3/3 | 5 Windows-VM UAT items persist — expected |
| 4. Telemetry + Remote Config | passed (13/13) | 3/3 | OPS-03 scope-adjusted to per-scan fetch |

## Requirements Coverage

All 30 active-scope REQ-IDs satisfied or satisfied-with-scope-adjustment:

| Category | Count | Status |
|----------|-------|--------|
| SCAF-01..06 | 6 | ✅ satisfied |
| SCAN-01..14 | 14 | ✅ satisfied |
| UI-06..09 (installer-native) | 4 | ✅ satisfied (wizard flow runtime UAT outstanding) |
| INST-01..04, INST-08..11 | 8 | ✅ satisfied (runtime UAT outstanding) |
| OPS-01, OPS-02, OPS-06, OPS-07 | 4 | ✅ satisfied |
| OPS-03, OPS-04, OPS-05 | 3 | ✅ satisfied via per-scan path (scope-adjusted, documented) |

Cut from scope (16 REQ-IDs): TRAY-01..08, UI-01..05, UI-10, INST-05..07.

## Cross-Phase Integration

17/17 exports wired; 0 orphaned; 0 routes without callers; all auth boundaries HMAC-protected.

End-to-end flows verified (structural):
1. Marco runs installer → wizard → Scheduled Task → first scan → telemetry captures any panic ✅
2. Server admin edits remote_config.json → next scan picks up new webhook_url within 5 min ✅
3. CI pipeline: test → build-windows → build-installer → signed artifacts uploaded ✅
4. Binary launched by Scheduled Task → telemetry.Wrap catches panics → POST to /telemetry ✅
5. `earlscheib.exe --scan` matches Python `ems_watcher.py --loop` byte-for-byte on HMAC signatures (cross-language parity test in CI) ✅

## Deployment Action Items (post-audit)

1. **OV cert procurement** — kick off now (2–10 business days; SCAF-06). Provision into CI HSM as `SIGNING_CERT_B64` / `SIGNING_CERT_PASS` secrets.
2. **Server deploy** — `systemctl restart app` on `support.jjagpal.me` after app.py changes land (new routes + Twilio switch comment).
3. **Windows VM smoke** — run `EarlScheibWatcher-Setup.exe` on a clean Windows 10 VM; verify Phase 3 HUMAN-UAT items.
4. **Twilio switch** — when ready for production SMS, follow the documented comment block in `app.py` `send_sms()` (change TWILIO_FROM, drop `whatsapp:` prefix).

## Recommendation

**SHIP v1.0** once (a) OV cert is in CI secrets and (b) Windows VM smoke passes the 5 HUMAN-UAT items in Phase 3. Code and tests are ready; remaining work is procurement + manual smoke + server deploy.

Cosmetic issues (heartbeat TrimRight, app.py dead code, no-op mv) can be cleaned up post-ship or in a v1.0.1 patch.
